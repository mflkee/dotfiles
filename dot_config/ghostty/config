## Minimal Ghostty config (managed by chezmoi)

# Font: match Waybar family
font-family = "Lilex Nerd Font Mono"

# Colors: pure black background, light gray text
background = #000000
foreground = #d0d0d0

# Font size (adjust to taste)
font-size = 14

# Note: If Ghostty still errors, comment options one-by-one to isolate.
# Some builds may not support extra keys like selection/cursor tweaks yet.

# Disable the "confirm on close" prompt
# In Ghostty the setting is named "confirm-close-surface".
confirm-close-surface = false
